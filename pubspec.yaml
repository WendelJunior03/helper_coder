name: helper_coder
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.0

  english_words: ^4.0.0
  provider: ^6.0.0
  google_generative_ai: ^0.4.6
  markdown_widget: ^2.3.2+6
  slide_to_act: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/images/IAAmigavel-semfundo.png
